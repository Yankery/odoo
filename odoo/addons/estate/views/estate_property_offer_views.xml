<odoo>
    <!-- List View -->
    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
        <list class="text-center">
            <field name="price" string="Offer Price" width="40%" class="text-center"/>
            <field name="status" string="Status" width="30%" class="text-center"/>
            <field name="partner_id" string="Offered By" width="30%" class="text-center"/>
        </list>
        </field>
    </record>


    <!-- Form View -->
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <header>
                    <button name="action_accept" type="object" string="Accept" invisible="status != 'new'"/>
                    <button name="action_refuse" type="object" string="Refuse" invisible="status != 'new'"/>
                </header>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="status"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="property_id"/>
                        <field name="partner_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
